# Provider modules

External provider adapters will be added in their approved phases. Raw provider response types must remain inside their adapter and product logic must consume normalized, provenance-bearing domain models.
